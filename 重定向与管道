#一、FD（文件描述符）简介
FD = file descriptors = 文件描述符 / 文件句柄进程用来管理、访问打开文件的数字标识。
固定 FD 含义
0：标准输入 stdin（键盘，只读）
1：标准输出 stdout（终端 / 屏幕）
2：标准错误 stderr（终端 / 屏幕）
3+：自定义文件 / 设备文件（可读可写）
关键结论程序通过 FD 访问资源，既可以是普通文件，也可以是设备文件。查看进程 FD：ls -l /proc/[PID]/fd
##二、输出重定向
把命令的输出写入文件，而不是打印在屏幕。
正确输出（FD 1）
> 文件名 覆盖写入（等价 1>）
>> 文件名 追加写入（等价 1>>）
错误输出（FD 2）
2> 文件名 覆盖写入错误信息
2>> 文件名 追加写入错误信息
正确 + 错误输出到同一位置
bash
运行
命令 &> 文件名
命令 > 文件名 2>&1
#三、输入重定向
从文件读取内容，代替键盘手动输入。
< 等价于 0<
典型场景：用文件内容直接发送邮件
#四、管道 |
作用把前一个命令的标准输出，作为后一个命令的标准输入。一次串联多个命令完成复杂任务。
常用示例
bash
运行
ps aux | grep vim
#五、tee 管道（三通管道）
作用
一边交给下一个命令处理
一边保存一份副本到文件
示例
bash
运行
cat /etc/passwd | tee 888.txt | tail -1
屏幕只显示最后一行
888.txt 保存完整全部内容
#六、xargs 参数传递
有些命令不支持直接接收管道数据，必须用 xargs 传递参数。
bash
运行
find ... | xargs rm
#七、统计命令 wc
统计文件的行数、单词数、字节数。
wc -l 统计总行数（最常用）
wc -w 统计单词数
wc -c 统计字节数
**整章一句话记忆**
FD 是进程访问文件的编号，0/1/2 系统默认；
重定向读写文件，管道串联命令；
tee 存副本，xargs 传参数，wc 做统计。
