#一、自有服务概述：
##   服务： 是运行在系统后台的特定进程，用于持续监听并处理用户或其他程序的请求。
##   自有服务：指Linux 系统开机后就自动启动、后台常驻运行的系统自带服务。不需要用户手动启动，开机就运行，随时可以对外提供功能。
##   作用：当客户端发起请求时，这些后台进程会自动响应并提供服务。
##   举例：
SSH 服务：开机自启，监听 22 端口
当我们用 SSH 客户端连接 Linux 时，服务器能响应，就是因为 sshd 服务一直在后台运行。

⭐一句话理解：自有服务 = Linux 开机就自动跑在后台、随时提供服务的程序。

#二、systemctl管理系统服务
## systemctl概述 systemctl start|stop|restart 服务名
                 systemctl命令 = system系统 + control控制（服务管理+开机启动项管理）













# systemd 服务管理

## 常用命令
- 查看状态：`systemctl status crond`
- 启动服务：`systemctl start crond`
- 开机自启：`systemctl enable --now crond`
- 禁用服务：`systemctl disable --now crond`





































































