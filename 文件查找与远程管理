补充： 
##date命令：获取当前时间 : date "+%Y-%m-%d %H-%M-%S"

##查看某条命令权限：
1：which 命令 → 找到路径
2：ls -l 路径 → 查看权限

##用 ls -lhd 目录 看到的目录大小，只是目录本身存储 “文件名 + inode 号” 的 block 大小（通常只有 4KB），
完全不包含里面文件的真实大小！！
ls -lhd 目录	目录本身的 block 大小（只存目录项）
du -sh 目录	目录 + 所有子文件 / 子目录的总占用空间，递归计算目录+所有子内容的真实磁盘占用，是运维/排查的标准用法。

## rm etc*   删除etc开头的所有文件

#一、文件查找
命令	|作用	                     |搜索范围	            |速度	            |特点
which	|查找命令/可执行文件 路径	 |只搜索 $PATH 目录	  |极快	              |专门找命令，不搜普通文件
locate|快速按文件名搜文件 / 目录	 |全系统（依赖数据库）	|很快	            |非实时，新文件需 updatedb
find	|强大、精确搜索文件 / 目录	 |任意指定目录	        |较慢（实时扫盘）	|支持按名称、类型、权限、时间、用户等条件查找

##1.1
/usr/bin/目录（所有在 PATH 里的目录）中创建一个文件，在给文件执行权限x后，也可以通过which找到
##1.2.1
locate查找文件hosts文件：# locate hosts
更新locate数据库：updatedb
案例：# touch hello.txt
# locate hello.txt
'''发现找不到这个文件'''
# updatedb
# locate hello.txt
'''可以找到文件了'''

##语法⭐find [path...] [options]  [expression] 				[action]
        命令 路径       选项             表达式        动作

-i忽略大小写
-name按文件名查找
-size按文件大小查找

# find /etc -size +5M
# find /etc -size 5M
# find /etc -size -5M

+5M		文件>5M
5M		文件=5M
-5M		文件<5M

# 生成指定大小的文件 if是从哪个文件中获取内容，of是将获取的内容写入到哪个文件
# bs是每次写入多大的内容，count是写几次
# dd if=/dev/zero of=/etc/1.txt bs=1M count=5

# 需求：查询/etc中大小是3M的文件，并且将查找的文件详细展示出来
# find /etc -size 3M | xargs ls -lh

-maxdepth （数字） 查找的目录深度












#二，文件打包及压缩
##1语法⭐tar 选项 压缩包名称 源目录  

##2打包&压缩
# tar -_cf etc.tar._ /etc  （根据需要补充）
c：表示打包
f：表示打包压缩后的文件名称，一般放在最后面
z：表示使用gzip压缩工具压缩，后缀是xxx.tar.gz （体积最大）
j：表示使用bz2压缩工具压缩，后缀是xxx.tar.bz  （体积较小）
J：表示使用xz压缩工具压缩，后缀是xxx.tar.xz   （体积最小）

##3解压&解包
查看包内容（不解压）tar -tf 包名.tar.gz










#三，网络信息基本管理
3.1 查看IP网卡信息： ifconfig
3.2 查看网络状态： nmcli device status
3.3 关闭网络：nmcli connection down ens33
3.4 开启网络：nmcli connection up ens33











